<%= simple_form_for(@contact_entry,
  :as => :contact,
  :html => { :method => :post },
  :url => entity_contacts_path(@entity),
  :wrapper => :basic_form) do |form| %>
<header>
  <div class="container">
    <h1>New Contact</h1>
    <nav>
      <input type="submit" value="Save" />
      <%= link_to 'Cancel', entity_path(@entity) %>
    </nav>
  </div>
</header>
<section class="container">
  <%= render :partial => 'form',
             :locals => { :form => form, :model => @contact_entry } %>
</section>
<% end %>
