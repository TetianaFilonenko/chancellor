<%= simple_form_for(@contact,
  :wrapper => :basic_form) do |form| %>
<header>
  <div class="container">
    <h1>Edit Contact</h1>
    <nav>
      <input type="submit" value="Save" />
      <%= link_to 'Cancel', entity_path(@contact.entity) %>
    </nav>
  </div>
</header>
<section class="container">
  <%= render :partial => 'form',
             :locals => { :form => form, :model => @contact } %>
</section>
<% end %>
